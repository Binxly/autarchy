#!/bin/bash

if [[ "$1" == "-y" ]] || gum confirm "Refresh Plymouth theme? This will replace your current startup screen with Autarchy defaults."; then
  sudo cp ~/.local/share/autarchy/default/plymouth/* /usr/share/plymouth/themes/autarchy/
  sudo plymouth-set-default-theme -R autarchy
fi
